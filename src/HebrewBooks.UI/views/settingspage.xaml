<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="HebrewBooks.UI.Views.SettingsPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml" xmlns:res="clr-namespace:HebrewBooks.UI.Resources" xmlns:conv="clr-namespace:HebrewBooks.UI.Converters" xmlns:beh="clr-namespace:HebrewBooks.UI.Behaviors" xmlns:m="clr-namespace:HebrewBooks.UI.Markup" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.CanContentScroll="False" Title="SettingsPage" xmlns:views="clr-namespace:HebrewBooks.UI.Views" FlowDirection="{m:LangFlow}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <BooleanToVisibilityConverter x:Key="BoolToVis" />
      <conv:ThemeNameConverter x:Key="ThemeNameConverter" />
      <conv:MissingBookActionNameConverter x:Key="MissingBookActionNameConverter" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <ScrollViewer VerticalScrollBarVisibility="Auto" beh:WheelScrollSpeed.LinesPerNotch="2">
    <StackPanel Margin="16">
      <ui:TextBlock Text="{x:Static res:SharedStrings.SettingsTitle}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S384}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <CheckBox Content="{x:Static res:SharedStrings.S385}" ToolTip="{x:Static res:SharedStrings.S386}" Margin="0,0,0,8" IsChecked="{Binding UnifiedSearchLayout, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S387}" Margin="0,0,0,8" IsChecked="{Binding ShowRowDetails, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S388}" ToolTip="{x:Static res:SharedStrings.S389}" Margin="0,0,0,8" IsChecked="{Binding ShowSearchHint, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S390}" ToolTip="{x:Static res:SharedStrings.S391}" Margin="0,0,0,8" IsChecked="{Binding EnableSynonymChips, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.PerfSettingsToggle}" ToolTip="{x:Static res:SharedStrings.PerfSettingsToggleTip}" Margin="0,0,0,8" IsChecked="{Binding ShowPerformanceHints, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S392}" ToolTip="{x:Static res:SharedStrings.S393}" Margin="0,0,0,8" IsChecked="{Binding ShowPageRail, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S394}" ToolTip="{x:Static res:SharedStrings.S395}" Margin="0,0,0,8" IsChecked="{Binding ShowHitPagesStrip, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S396}" ToolTip="{x:Static res:SharedStrings.S397}" Margin="0,0,0,8" IsChecked="{Binding PersistSessionState, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S398}" ToolTip="{x:Static res:SharedStrings.S399}" Margin="0,0,0,8" IsChecked="{Binding ShareUsageData, Mode=TwoWay}" />
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S400}" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding ThemeOptions}" SelectedItem="{Binding SelectedTheme, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock>
                    <TextBlock.Text>
                      <PriorityBinding>
                        <Binding Path="." Converter="{StaticResource ThemeNameConverter}" />
                        <Binding Path="." />
                      </PriorityBinding>
                    </TextBlock.Text>
                  </TextBlock>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ComboBox>
          </Grid>
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S401}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S402}" />
            <ComboBox Grid.Column="1" ItemsSource="{Binding MissingBookActions}" SelectedItem="{Binding SelectedMissingBookAction, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
              <ItemsControl.ItemTemplate>
                <DataTemplate>
                  <TextBlock Text="{Binding Converter={StaticResource MissingBookActionNameConverter}}" />
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ComboBox>
          </Grid>
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S403}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S404}" />
            <ComboBox Grid.Column="1" IsEditable="True" ToolTip="{x:Static res:SharedStrings.S405}" ItemsSource="{Binding RegionCopyDpiPresets}" Text="{Binding RegionCopyDpi, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
          </Grid>
          <ui:Button Content="{x:Static res:SharedStrings.S406}" HorizontalAlignment="Right" Margin="0,12,0,0" ToolTip="{x:Static res:SharedStrings.S407}" Command="{Binding ResetViewSettingsCommand}" />
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S408}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S409}" VerticalAlignment="Center" />
            <ComboBox Grid.Column="1" MinWidth="260" HorizontalAlignment="Left" DisplayMemberPath="Display" SelectedValuePath="Value" ItemsSource="{Binding LanguageChoices}" SelectedValue="{Binding SelectedLanguage, Mode=TwoWay}" />
          </Grid>
          <TextBlock Text="{x:Static res:SharedStrings.S410}" Opacity="0.75" Margin="0,8,0,0" TextWrapping="Wrap" />
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S411}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.OptionProximity}" VerticalAlignment="Center" />
            <ui:NumberBox Grid.Column="1" Minimum="1" Maximum="999" FlowDirection="LeftToRight" HorizontalAlignment="Left" MinWidth="120" Value="{Binding MaxProximity}" />
          </Grid>
          <CheckBox Content="{x:Static res:SharedStrings.OptionHybur}" Margin="0,8,0,0" IsChecked="{Binding Hybur, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.OptionRasheyTevot}" Margin="0,8,0,0" IsChecked="{Binding RasheyTevot, Mode=TwoWay}" />
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S412}" VerticalAlignment="Center" />
            <ui:NumberBox Grid.Column="1" Minimum="10" Maximum="10000" FlowDirection="LeftToRight" HorizontalAlignment="Left" MinWidth="120" Value="{Binding MaxFilesToRetrieve}" />
          </Grid>
          <Grid Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S413}" VerticalAlignment="Center" />
            <Border Grid.Column="1" Width="32" Height="24" Margin="0,0,8,0" BorderBrush="#888" BorderThickness="1" CornerRadius="3" Background="{Binding HighlightColorBrush}" />
            <ui:Button Grid.Column="2" Content="{x:Static res:SharedStrings.S414}" MinWidth="120" Command="{Binding PickHighlightColorCommand}" />
            <ui:Button Grid.Column="3" Content="{x:Static res:SharedStrings.S415}" MinWidth="120" Margin="8,0,0,0" Command="{Binding ResetHighlightColorCommand}" />
          </Grid>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="0">
        <Expander Padding="16" IsExpanded="False">
          <HeaderedContentControl.Header>
            <TextBlock Text="{x:Static res:SharedStrings.S416}" FontWeight="Bold" FontSize="16" />
          </HeaderedContentControl.Header>
          <StackPanel Margin="0,8,0,0">
            <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,8" Text="{x:Static res:SharedStrings.S417}" />
            <ui:TextBox PlaceholderText="{x:Static res:SharedStrings.S418}" Margin="0,0,0,8" Text="{Binding RasheyTevotFilter, UpdateSourceTrigger=PropertyChanged}" />
            <ui:DataGrid AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column" GridLinesVisibility="Horizontal" Height="280" ItemsSource="{Binding RasheyTevotEntries}" SelectedItem="{Binding SelectedRasheyTevotEntry, Mode=TwoWay}" FlowDirection="{m:LangFlow}">
              <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static res:SharedStrings.S419}" Width="160" Binding="{Binding Acronym, UpdateSourceTrigger=PropertyChanged}" />
                <DataGridTextColumn Header="{x:Static res:SharedStrings.S420}" Width="*" Binding="{Binding Expansions, UpdateSourceTrigger=PropertyChanged}" />
              </DataGrid.Columns>
            </ui:DataGrid>
            <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
              <ui:Button Content="{x:Static res:SharedStrings.S421}" MinWidth="110" Margin="0,0,8,0" Command="{Binding AddRasheyTevotEntryCommand}" />
              <ui:Button Content="{x:Static res:SharedStrings.S422}" MinWidth="110" Margin="0,0,8,0" Command="{Binding RemoveRasheyTevotEntryCommand}" />
              <ui:Button Content="{x:Static res:SharedStrings.S423}" MinWidth="110" Margin="0,0,8,0" Command="{Binding ReloadRasheyTevotCommand}" />
              <ui:Button Content="{x:Static res:SharedStrings.S424}" Appearance="Primary" MinWidth="160" Command="{Binding SaveRasheyTevotCommand}" />
            </StackPanel>
            <TextBlock Margin="0,8,0,0" Opacity="0.8" TextWrapping="Wrap" Text="{Binding RasheyTevotStatus}" />
          </StackPanel>
        </Expander>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S425}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,8" Text="{x:Static res:SharedStrings.S426}" />
          <TextBlock Margin="0,0,0,8" Opacity="0.8" TextWrapping="Wrap" Text="{Binding TocBundleStatus}" />
          <StackPanel Orientation="Horizontal">
            <ui:Button Content="{x:Static res:SharedStrings.S427}" Appearance="Primary" MinWidth="140" Margin="0,0,8,0" Command="{Binding ExportTocsCommand}" />
            <ui:Button Content="{x:Static res:SharedStrings.S428}" MinWidth="140" Command="{Binding ImportTocsCommand}" />
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S429}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,8" FontSize="12" Text="{x:Static res:SharedStrings.S430}" />
          <CheckBox Content="{x:Static res:SharedStrings.S431}" ToolTip="{x:Static res:SharedStrings.S432}" Margin="0,0,0,8" IsChecked="{Binding UseOnlineService, Mode=TwoWay}" />
          <StackPanel Visibility="{Binding UseOnlineService, Converter={StaticResource BoolToVis}}">
            <Grid Margin="0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S433}" VerticalAlignment="Center" />
              <ui:TextBox Grid.Column="1" FlowDirection="LeftToRight" Text="{Binding OnlineServiceUrl, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" PlaceholderText="{Binding OnlineServiceUrlPlaceholder}" />
            </Grid>
            <Grid Margin="0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S434}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S435}" />
              <ui:TextBox Grid.Column="1" FlowDirection="LeftToRight" Text="{Binding OnlinePdfBaseUrl, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" PlaceholderText="{Binding OnlinePdfBaseUrlPlaceholder}" />
            </Grid>
            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
              <ui:Button Content="{x:Static res:SharedStrings.S436}" Command="{Binding CheckOnlineConnectionCommand}" />
              <TextBlock VerticalAlignment="Center" Margin="12,0,0,0" Opacity="0.85" Text="{Binding OnlineConnectionStatus}" />
            </StackPanel>
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.IndexLocationTitle}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,8" FontSize="12" Text="{x:Static res:SharedStrings.IndexLocationDesc}" />
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.IndexLocationLabel}" VerticalAlignment="Center" />
            <ui:TextBox Grid.Column="1" FlowDirection="LeftToRight" PlaceholderText="C:\HebrewBooks\Bookshelf_IDX" Text="{Binding FastIndexesDir, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
            <ui:Button Grid.Column="2" Margin="8,0,0,0" Content="{x:Static res:SharedStrings.IndexLocationBrowse}" Command="{Binding BrowseFastIndexDirCommand}" />
          </Grid>
          <TextBlock TextWrapping="Wrap" FlowDirection="LeftToRight" HorizontalAlignment="Left" Opacity="0.85" FontSize="12" Margin="0,6,0,0" Text="{Binding IndexLocationStatus}" />
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S437}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,8" FontSize="12" Text="{x:Static res:SharedStrings.S438}" />
          <CheckBox Content="{x:Static res:SharedStrings.S439}" ToolTip="{x:Static res:SharedStrings.S440}" FontWeight="SemiBold" Margin="0,4,0,8" IsChecked="{Binding NetworkInstall, Mode=TwoWay}" />
          <StackPanel Visibility="{Binding NetworkInstall, Converter={StaticResource BoolToVis}}">
            <Grid Margin="0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="90" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S441}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S442}" />
              <ui:TextBox Grid.Column="1" PlaceholderText="192.168.1.20" Text="{Binding SearchServiceHost, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
              <TextBlock Grid.Column="2" Text="{x:Static res:SharedStrings.S443}" VerticalAlignment="Center" Margin="12,0,8,0" />
              <ui:TextBox Grid.Column="3" PlaceholderText="8080" Text="{Binding SearchServicePort, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
            </Grid>
            <Grid Margin="0,4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
              </Grid.ColumnDefinitions>
              <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S444}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S445}" />
              <ui:TextBox Grid.Column="1" PlaceholderText="\\192.168.1.20\f\HebrewBooks" Text="{Binding NetworkBasePath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
            </Grid>
            <CheckBox Content="{x:Static res:SharedStrings.S446}" ToolTip="{x:Static res:SharedStrings.S447}" Margin="0,8,0,0" IsChecked="{Binding ForceProtectMode, Mode=TwoWay}" />
            <Expander Header="{x:Static res:SharedStrings.S448}" Margin="0,10,0,0">
              <StackPanel Margin="0,8,0,0">
                <TextBlock TextWrapping="Wrap" Opacity="0.7" FontSize="11" Margin="0,0,0,6" Text="{x:Static res:SharedStrings.S449}" />
                <Grid Margin="0,4">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S450}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S451}" />
                  <ui:TextBox Grid.Column="1" PlaceholderText="\\server\HebrewBooks\Books" Text="{Binding BooksDirOverride, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                </Grid>
                <Grid Margin="0,4">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S452}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S453}" />
                  <ui:TextBox Grid.Column="1" PlaceholderText="\\server\HebrewBooks\App\Katalog.db" Text="{Binding CatalogMasterPath, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                </Grid>
                <Grid Margin="0,4">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S454}" VerticalAlignment="Center" ToolTip="{x:Static res:SharedStrings.S455}" />
                  <ui:TextBox Grid.Column="1" PlaceholderText="http://192.168.1.20:8080" Text="{Binding SearchServiceUrl, Mode=TwoWay, UpdateSourceTrigger=LostFocus}" />
                </Grid>
              </StackPanel>
            </Expander>
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16" Visibility="{m:HideInProtectMode}">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S456}" FontWeight="Bold" FontSize="16" Margin="0,0,0,4" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" FontSize="12" Margin="0,0,0,8" Text="{x:Static res:SharedStrings.S457}" />
          <TextBlock Margin="0,0,0,8" Opacity="0.85" TextWrapping="Wrap" Text="{Binding OcrEngineStatus}" />
          <ProgressBar Maximum="100" Height="6" Margin="0,0,0,8" Value="{Binding OcrInstallProgress}" Visibility="{Binding OcrShowProgress, Converter={StaticResource BoolToVis}}" />
          <StackPanel Orientation="Horizontal">
            <ui:Button Appearance="Primary" MinWidth="200" Margin="0,0,8,0" Content="{Binding OcrEngineInstallButtonText}" Command="{Binding InstallOcrEngineCommand}" IsEnabled="{Binding OcrControlsEnabled}" />
            <ui:Button Content="{x:Static res:SharedStrings.S458}" MinWidth="140" Command="{Binding OpenOcrReleasePageCommand}" IsEnabled="{Binding OcrControlsEnabled}" />
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S459}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <Grid Margin="0,4">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static res:SharedStrings.S460}" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="SemiBold" Text="{Binding CurrentVersion}" />
          </Grid>
          <CheckBox Content="{x:Static res:SharedStrings.S461}" Margin="0,8,0,0" IsChecked="{Binding CheckUpdatesOnStartup, Mode=TwoWay}" />
          <CheckBox Content="{x:Static res:SharedStrings.S462}" Margin="24,4,0,0" IsChecked="{Binding AutoDownloadUpdates, Mode=TwoWay}" IsEnabled="{Binding CheckUpdatesOnStartup}" />
          <CheckBox Content="{x:Static res:SharedStrings.S463}" Margin="0,8,0,0" IsChecked="{Binding IncludeBetaUpdates, Mode=TwoWay}" />
          <TextBlock Text="{x:Static res:SharedStrings.S464}" FontSize="11" Opacity="0.7" Margin="24,2,0,0" TextWrapping="Wrap" />
          <TextBlock Margin="0,8,0,0" Opacity="0.8" TextWrapping="Wrap" Text="{Binding UpdateStatus}" />
          <TextBlock Text="{x:Static res:SharedStrings.S317}" FontWeight="SemiBold" Margin="0,10,0,2" Opacity="0.8" Visibility="{Binding HasUpdateReleaseNotes, Converter={StaticResource BoolToVis}}" />
          <ScrollViewer MaxHeight="180" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Visibility="{Binding HasUpdateReleaseNotes, Converter={StaticResource BoolToVis}}">
            <TextBlock FontSize="12" Opacity="0.8" TextWrapping="Wrap" Text="{Binding UpdateReleaseNotes}" />
          </ScrollViewer>
          <ProgressBar Maximum="100" Height="6" Margin="0,8,0,0" Value="{Binding UpdateProgress}" Visibility="{Binding HasPendingUpdate, Converter={StaticResource BoolToVis}}" />
          <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
            <ui:Button Content="{x:Static res:SharedStrings.S465}" MinWidth="140" Margin="0,0,8,0" Command="{Binding CheckForUpdatesCommand}" />
            <ui:Button Content="{x:Static res:SharedStrings.S466}" Appearance="Primary" MinWidth="180" Command="{Binding InstallUpdateCommand}" Visibility="{Binding HasPendingUpdate, Converter={StaticResource BoolToVis}}" />
          </StackPanel>
          <Separator Margin="0,14,0,10" Opacity="0.5" Visibility="{Binding CanUninstall, Converter={StaticResource BoolToVis}}" />
          <ui:Button Content="{x:Static res:SharedStrings.S467}" Appearance="Caution" HorizontalAlignment="Right" ToolTip="{x:Static res:SharedStrings.S468}" Command="{Binding UninstallCommand}" Visibility="{Binding CanUninstall, Converter={StaticResource BoolToVis}}" />
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16" Visibility="{m:HideInProtectMode}">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S469}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,12" Text="{x:Static res:SharedStrings.S9049}" />
          <StackPanel Orientation="Horizontal">
            <ui:Button Content="{x:Static res:SharedStrings.S470}" MinWidth="200" Margin="0,0,8,0" Command="{Binding OpenDataFolderWizardCommand}" />
            <ui:Button Content="{x:Static res:SharedStrings.S471}" ToolTip="{x:Static res:SharedStrings.S472}" MinWidth="200" Command="{Binding ResetDataRootCommand}" />
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16" Visibility="{m:HideInProtectMode}">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S2389}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,12" Text="{x:Static res:SharedStrings.S2390}" />
          <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Text="{Binding LibraryDownloadStatus}" />
          <ProgressBar Height="6" Margin="0,0,0,10" Maximum="100" Value="{Binding LibraryDownloadProgress, Mode=OneWay}" Visibility="{Binding LibraryDownloadRunning, Converter={StaticResource BoolToVis}}" />
          <StackPanel Orientation="Horizontal">
            <ui:Button Content="{x:Static res:SharedStrings.S2391}" MinWidth="200" Margin="0,0,8,0" Command="{Binding ResumeLibraryDownloadCommand}" IsEnabled="{Binding LibraryDownloadHasWork}" />
            <ui:Button Content="{x:Static res:SharedStrings.S2393}" MinWidth="120" Margin="0,0,8,0" Command="{Binding CancelLibraryDownloadCommand}" IsEnabled="{Binding LibraryDownloadRunning}" />
            <ui:Button Content="{x:Static res:SharedStrings.S2392}" MinWidth="120" Command="{Binding RefreshLibraryDownloadStatusCommand}" />
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <ui:Card Margin="0,0,0,12" Padding="16" Visibility="{m:HideInProtectMode}">
        <StackPanel>
          <TextBlock Text="{x:Static res:SharedStrings.S473}" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
          <TextBlock TextWrapping="Wrap" Opacity="0.8" Margin="0,0,0,12" Text="{x:Static res:SharedStrings.S9050}" />
          <StackPanel Orientation="Horizontal">
            <ui:Button Content="{x:Static res:SharedStrings.S212}" Appearance="Caution" ToolTip="{x:Static res:SharedStrings.S474}" MinWidth="140" Margin="0,0,8,0" Command="{Binding ResetUserStateCommand}" />
            <ui:Button Content="{x:Static res:SharedStrings.S214}" Appearance="Danger" ToolTip="{x:Static res:SharedStrings.S475}" MinWidth="140" Command="{Binding HardResetUserStateCommand}" />
          </StackPanel>
        </StackPanel>
      </ui:Card>
      <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
        <ui:Button Content="{x:Static res:SharedStrings.ButtonOk}" Appearance="Primary" MinWidth="120" Command="{Binding SaveCommand}" />
      </StackPanel>
    </StackPanel>
  </ScrollViewer>
</Page>